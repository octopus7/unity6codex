# Install

Add the package as a local file dependency during package learning:

```json
"com.codexsix.request-pipeline": "file:../external/codexsix-request-pipeline"
```

This package is intentionally small so the folder layout is easy to inspect.

