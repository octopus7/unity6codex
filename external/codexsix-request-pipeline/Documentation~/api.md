# API

Minimal contracts included in this learning package:

- `IRequestClient`
- `IHttpTransport`
- `IBodySerializer`
- `IAuthProvider`
- `Request`, `Response`, `RequestOptions`

The package is meant to demonstrate package shape rather than complete runtime behavior.
