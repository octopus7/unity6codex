# Usage

The intended learning flow is:

1. Inspect `package.json`.
2. Open the runtime and editor `asmdef` files.
3. Compare `Runtime/`, `Editor/`, `Tests/`, `Samples~/`, and `Documentation~/`.
4. Import the sample and follow the script dependencies.

